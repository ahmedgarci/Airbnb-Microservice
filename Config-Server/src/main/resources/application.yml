server:
    port: 8000
spring:
    application:
        name: Config-Server
    profiles:
        active: native
    cloud:
        config:
            server: 
                native:
                    search-locations: classpath:/Configs

